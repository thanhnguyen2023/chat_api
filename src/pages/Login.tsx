import LoginForm from '@/components/login/LoginForm';
import React from 'react';

const LoginPage = () => {
    return (
        <div>
            <LoginForm />
        </div>
    );
};

export default LoginPage;